-r requirements.txt

# Test and tooling dependencies. Never deployed.
pytest>=8.0
pytest-cov>=5.0
# moto gives real DynamoDB semantics -- including ConditionExpression
# evaluation -- in-process. Hand-rolled dict mocks cannot test the dedupe,
# which is the one piece of DynamoDB logic in this project worth testing.
moto[dynamodb,sns,cloudwatch]>=5.0
ruff>=0.6
# The deploy scripts cannot be unit tested against a real account, so static
# analysis is what is left. Installed through pip rather than relying on the
# CI runner's preinstalled shellcheck, so the version that gates a pull
# request is the version a developer runs locally.
shellcheck-py>=0.10
